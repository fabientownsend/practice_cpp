#include <iostream>

using namespace std;

int main() {
  // { 3 < .begin(), 4, 5, 6} < .end()

  // sort(v.begin(), v.end())
  // reverse(v.begin(), v.end())
  // random_shuffle(v.begin(), v.end())

  // also use with array
  // sort(a, a + n)
  // reverse(a, a + n)
  // random_shuffle(a, a + n)

  // Set iterators
  // set<int>::iterator it = s.begin();
  // (equivalent) auto it = s.begin();
  // cout << *it << endl; // print the first element
  //
  // for (auto it = s.begin(); it != s.end(); it++) {
  // cout << *it << endl;
  // }

  // print the bigest element
  // auto it = s.end();
  // it--;
  // cout << *it << endl;
}
